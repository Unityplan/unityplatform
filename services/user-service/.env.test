# Test database configuration for user-service tests
DATABASE_URL=postgresql://unityplan:unityplan_dev_password_dk@localhost:5432/unityplan_dk

# Service configuration
HOST=127.0.0.1
PORT=8084
AVATARS_PATH=./test_uploads/avatars

# JWT configuration (for future auth integration tests)
JWT_SECRET=test_jwt_secret_key_for_testing_only
